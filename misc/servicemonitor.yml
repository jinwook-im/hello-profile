apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sampleapp
  labels:
    release: sampleapp
spec:
  selector:
    matchLabels:
      app: hello-blue
  endpoints:
    - port: metrics